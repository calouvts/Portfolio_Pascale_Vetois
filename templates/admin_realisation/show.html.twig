{% extends 'layoutadmin.html.twig' %}

{% block title %}Realisation{% endblock %}

{% block body %}
    <h1>Realisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ realisation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ realisation.name }}</td>
            </tr>
            <tr>
                <th>Realisationphoto</th>
                <td>{{ realisation.realisationphoto }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ realisation.description }}</td>
            </tr>
            <tr>
                <th>Projectlink</th>
                <td>{{ realisation.projectlink }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary mt-5" href="{{ path('admin_realisation_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-primary mt-5" href="{{ path('admin_realisation_edit', {'id': realisation.id}) }}">Modifier</a>

    {{ include('admin_realisation/_delete_form.html.twig') }}
{% endblock %}
