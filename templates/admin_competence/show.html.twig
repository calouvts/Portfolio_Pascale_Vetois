{% extends 'layoutadmin.html.twig' %}
{% block title %}Competence{% endblock %}

{% block body %}
    <h1>Competence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ competence.name }}</td>
            </tr>
            <tr>
                <th>Photocomp</th>
                <td>{{ competence.photocomp }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ competence.description }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-outline-primary mt-5" href="{{ path('admin_competence_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-primary mt-5" href="{{ path('admin_competence_edit', {'id': competence.id}) }}">Modifier</a>


    {{ include('admin_competence/_delete_form.html.twig') }}
{% endblock %}
